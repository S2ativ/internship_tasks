1.5 Регулярные выражения


Задание 1 
импортирую библиотеку для работы с регулярными выражениями
объявляю функцию
в переменную попадают все найденные адреса
возвращаю и вывожу результат

Задание 2 
импортирую библиотеку для поиска по шаблону
также импортирую библиотеку 'datetime' для  работы с датами 
объявляю функцию
объявляю паттерн,по которому будет происходить поиск дат двух типов:числовых и текстовых
все подходящие результаты сохраняются в 'all_dates'
создаю список,в который буду записывать окончательно готовые даты 
прохожусь циклом по всем датам
если в элементе уже есть точка,значит дата записана верно и ее можно добавить в готовые даты 'form_dates'
иначе дата будет 'расшифровываться' с помощью функции datetime.strptime в параметры которой будут входить (месяц день,год)
добавляю в готовый список дату,которую перевожу в стандартный вид с помощью strftime
вывожу результат